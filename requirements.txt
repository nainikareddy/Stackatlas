# StackAtlas pipeline + MCP server + eval harness
psycopg2-binary>=2.9      # introspect.py: Postgres introspection
mcp[cli]>=1.2,<2          # mcp_server/server.py: FastMCP server (v1 API; v2 renamed FastMCP->MCPServer)
jsonschema>=4.0           # evals/verify.py: catalog schema validation
# docgen.py and the SQL eval runner shell out to `claude -p` (Claude Code CLI,
# subscription auth) instead of the anthropic SDK — no API key required.
